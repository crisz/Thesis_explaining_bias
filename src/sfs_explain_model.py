

def get_flip_difference(words_arr, flip_indices):
    # pred_before = model.predict(words_arr)
    # for index in flip_indices:
    #   words_arr[index] = word2vec.invert(words_arr[index])
    # pred_after = model.predict(words_arr)
    # return (pred_before[1] - pred_after[1])**2